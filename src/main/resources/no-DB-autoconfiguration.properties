# The current mainline doesn't need a data source. While this version has removed spring.boot.starter.data.jpa from the POM, I'm still getting error messages about DB config...
# May eventually wish to move to @SpringBootApplication(exclude = {DataSourceAutoConfiguration.class, ...}) =for the mainline= or any other branch which doesn't require JPA/DB.
spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration 